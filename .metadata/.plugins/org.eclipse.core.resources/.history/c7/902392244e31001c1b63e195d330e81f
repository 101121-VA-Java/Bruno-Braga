package main;

import java.time.LocalDate;
import java.util.Scanner;

import com.revature.Task.*;


public class Main {

	public static void main(String[] args) {
		
		Scanner imput = new Scanner(System.in);
		LocalDate dueDate = new LocalDate(entry);
	}

	// prompt: please enter the year of your task due date
	// Scanner the imput gets the year date
	// validate the Scanned imput against a valid year
	// if is pass store it
	
		// else print "invalid year"
	
	// prompt: set a due date in the following format "yyyy-mm-dd" 
		
	
	
	
	 
    
    
    public void loginScreen() {
		String uname = "admin";
		String pass = "pass";
		
		System.out.println("Please enter the Taks' name:");
		String username = .nextLine();
		System.out.println("Please enter your password:");
		String password = sc.nextLine();
		if(uname.equals(username) && pass.equals(password)) {
			System.out.println("You're logged in.");
		} else {
			System.out.println("Not quite.");
		}
    }
	
}
